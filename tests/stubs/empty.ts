// Empty stub. Aliased in place of the `server-only` package for integration
// tests so server-only modules (e.g. lib/articleList) can be imported and
// unit-tested in the node test environment.
export {}
